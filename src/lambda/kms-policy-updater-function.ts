// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for KmsPolicyUpdaterFunction
 */
export interface KmsPolicyUpdaterFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/lambda/kms-policy-updater.
 */
export class KmsPolicyUpdaterFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: KmsPolicyUpdaterFunctionProps) {
    super(scope, id, {
      description: 'src/lambda/kms-policy-updater.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/lambda/kms-policy-updater.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}